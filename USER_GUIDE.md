# 블로그 사용 가이드

---

## 1. 준비물 (최초 1회 설정)
컴퓨터에 아래 프로그램들이 설치되어 있어야 합니다.
1. **Antigravity**: 글을 수정하고 명령어를 입력하는 IDE(편집기)입니다.
2. **Node.js**: 블로그 시스템을 돌리는 엔진입니다.
3. **GitHub**: 블로그 코드가 저장된 웹사이트입니다.

---

## 2. 블로그 시작하기 (미리보기)
글을 쓰기 전에 화면에 어떻게 나올지 확인하는 방법입니다.

1. **Antigravity**에서 `hj-log` 폴더를 엽니다.
2. 상단 메뉴의 **터미널(Terminal) -> 새 터미널(New Terminal)**을 클릭합니다.
3. 아래 명령어를 입력하고 키보드의 **Enter**를 누릅니다.
   ```bash
   npm run dev
   ```
4. 터미널 창에 나타난 `http://localhost:5173` 주소를 **Ctrl (또는 Cmd) + 클릭**하면 브라우저에서 블로그가 열립니다.

---

## 3. 새로운 글 작성하기

모든 글은 `src/posts` 폴더 안에 저장됩니다.

1. `src/posts` 폴더 위에 마우스 오른쪽 버튼을 누르고 **새 파일(New File)**을 클릭합니다.
2. 파일 이름을 정합니다. (예: `my-first-story.md`)
   - **주의**: 반드시 끝에 `.md`라고 적어주세요.
3. 파일의 **맨 윗부분**에 아래 내용을 복사해서 붙여넣고 내용을 수정하세요. (이 부분을 '설정값'이라고 부릅니다.)

```markdown
---
title: "나의 첫 번째 글 제목"
date: "2026-02-14"
category: "일상"
tags: ["일기", "첫글"]
status: "COMPLETED"
summary: "이 글이 목록에서 어떻게 보일지 한 줄로 요약해 주세요."
---

이 아래부터는 자유롭게 글을 작성하시면 됩니다!
```

### 💡 작성 팁 (마크다운 문법)
- `# 제목`: 큰 제목
- `## 제목`: 중간 제목
- `**글자**`: **굵은 글씨**
- `- 항목`: 리스트(점)

---

## 4. 이미지 첨부하기

글에 사진이나 이미지를 넣고 싶을 때 사용합니다.

1. **이미지 파일 저장**: 넣고 싶은 이미지 파일을 `public/images` 폴더 안에 넣습니다.
2. **글에 넣기**: 글 작성 중에 이미지를 넣고 싶은 위치에 아래와 같이 입력합니다.
   ```markdown
   ![사진설명](/images/파일명.jpg)
   ```
   - 예: `public/images` 폴더에 `sky.jpg`가 있다면 `![하늘사진](/images/sky.jpg)` 라고 씁니다.

---

## 5. 글 수정 및 삭제하기

### 📝 글 수정
1. `src/posts` 폴더에서 고치고 싶은 파일을 클릭합니다.
2. 내용을 수정하고 **Ctrl + S (또는 Cmd + S)**를 눌러 저장합니다.
3. 실행 중인 브라우저 창에서 바로 내용이 바뀌는 것을 확인할 수 있습니다.

### 🗑️ 글 삭제
1. 지우고 싶은 파일 위에서 마우스 오른쪽 버튼을 누르고 **삭제(Delete)**를 클릭합니다.

---

## 6. 저장 및 배포 (남들에게 보여주기)
글을 다 썼다면 인터넷에 올려야 합니다. (GitHub를 사용하는 경우)

1. 터미널 창에 아래 명령어들을 순서대로 입력합니다.
   ```bash
   git add .
   git commit -m "새로운 글 작성 및 이미지 추가"
   git push
   ```
2. 이제 잠시 후 인터넷 주소로 접속하면 글이 올라가 있을 거예요!

---

## 🆘 도움이 필요하신가요?
- 명령어를 입력했는데 빨간 글씨가 나오면 `npm install`을 한 번 입력해 보세요.
- 설정값(맨 위의 `---` 사이)의 형식이 틀리면 에러가 날 수 있으니 오타에 주의해 주세요!
- 날짜 형식은 반드시 `"YYYY-MM-DD"` 형태를 지켜주세요.
